import { Filterpipe1Pipe } from './filterpipe1.pipe';

describe('Filterpipe1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Filterpipe1Pipe();
    expect(pipe).toBeTruthy();
  });
});
